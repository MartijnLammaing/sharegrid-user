/**
 * Integration test — session timeout.
 *
 * Phase 1 timeout test (sendPrompt rejects with SessionTimeoutError) removed
 * in Phase 2. The Phase 2 equivalent (sendInferenceRequest timeout) is written
 * in user Phase 10 of the implementation plan.
 */

// Placeholder — no tests yet.
